.SUBDIRS:

OCAMLINCLUDES = ../lib

OCAML_LIBS[] +=
    ../lib/ml2mxml

FILES[] =
    utils
    verse
    bridge
    chorus

OCAMLPACKS[] =
    cow

clean:
    rm -rf *.cm* *~ .\#* *.o *.lib *.omc *.install *.tmp first first.run first_all first_all.run

.DEFAULT: $(OCamlProgram first, first $(FILES)) $(OCamlProgram first_all, first_all $(FILES))
